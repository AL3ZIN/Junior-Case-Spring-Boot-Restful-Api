server:
  port: 8888
cors:
  originPatterns: http://localhost:8080,http://localhost:4200
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/juniorcase_db?useTimezone=true&serverTimezone=UTC
    username: root
    password: 161103
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
springdoc:
  pathsToMatch:
    - /api/**
  swagger-ui:
    use-root-path: true
secret:
  key: ${JWT_SECRET:12345678}